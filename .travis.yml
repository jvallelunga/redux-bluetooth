sudo: false
env:
  - CXX=g++-4.8
language: node_js
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8
script:
  - npm run test
branches:
  only:
    - master
cache:
  directories:
    - node_modules
after_success: 
  - npm run ci:report:coverage