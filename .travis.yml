language: node_js
node_js:
  - "node"
before_install: 
  - sudo apt-get install python3.6 
  - sudo apt-get install build-essential
  - npm install -g node-gyp
script:
  - npm run test
branches:
  only:
    - master
cache:
  directories:
  - node_modules